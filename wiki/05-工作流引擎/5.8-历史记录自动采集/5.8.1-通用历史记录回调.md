> 来源：`docs/05-工作流引擎.md` 第 874 行 > 位置：[总目录](../../README.md) -> [05 - 工作流引擎](../README.md) -> 当前章节 > 相邻：[上一篇](README.md) | [下一篇](5.8.2-字段级变更仍然使用-Gorm-Hook.md) > 相关主题：[06 - 插件与钩子体系](../../06-插件与钩子体系/README.md) | [07 - AI 编程工具集成](../../07-AI编程工具集成/README.md) | [08 - Git 集成方案](../../08-Git集成方案/README.md) | [09 - 监控与反馈闭环](../../09-监控与反馈闭环/README.md) | [11 - 测试策略](../../11-测试策略/README.md) ### 5.8.1 通用历史记录回调  ```go // internal/fsm/history_hook.go  // afterAnyEvent — 所有 fsm 事件结束后自动记录历史 func (f *IssueFSM) afterAnyEvent(ctx context.Context, e *fsm.Event) {     // 记录状态变更     _ = f.db.WithContext(ctx).Create(&model.HistoryRecord{         TargetType: "issue",         TargetId:   e.Args[0].(*model.Issue).ID,         Action:     "status_changed",         FieldName:  "status",         OldValue:   mustJSON(map[string]string{"status": e.Src}),         NewValue:   mustJSON(map[string]string{"status": e.Dst}),         ChangedBy:  ctx.Value("userID").(string),     }).Error }  // enterState — 进入特定状态时的附加记录 func (f *IssueFSM) enterState(ctx context.Context, e *fsm.Event) {     issue := e.Args[0].(*model.Issue)     // 例如进入 deployed 状态额外记录     if e.Dst == "deployed" {         f.svc.HookRegistry.Emit(ctx, HookIssueDeployed, issue)     } }  // leaveState — 离开状态时的清理逻辑 func (f *IssueFSM) leaveState(ctx context.Context, e *fsm.Event) {     // 离开 running 状态时，关闭 WebSocket 连接等     if e.Src == "running" {         f.svc.WsHub.CloseConn(e.Args[0].(*model.Issue).ID)     } }  // TestCase 使用 after_event 统一记录状态变更 // 注意: 状态变更记录到 status 字段，审核状态变更通过回调同步更新 reviewStatus func (f *TestCaseFSM) afterTestCaseEvent(ctx context.Context, e *fsm.Event) {     tc := e.Args[0].(*model.TestCase)          // 记录主状态变更     _ = f.db.WithContext(ctx).Create(&model.HistoryRecord{         TargetType: "test_case",         TargetId:   tc.ID,         Action:     "status_changed",         FieldName:  "status",         OldValue:   mustJSON(map[string]string{"status": e.Src}),         NewValue:   mustJSON(map[string]string{"status": e.Dst}),         ChangedBy:  ctx.Value("userID").(string),     }).Error          // submit 事件需要同步更新 reviewStatus (其他审核事件在专门的回调中处理)     if e.Event == "submit" {         f.db.Model(tc).Update("reviewStatus", "pending_review")                  _ = f.db.WithContext(ctx).Create(&model.HistoryRecord{             TargetType: "test_case",             TargetId:   tc.ID,             Action:     "status_changed",             FieldName:  "reviewStatus",             OldValue:   mustJSON(map[string]string{"reviewStatus": "draft"}),             NewValue:   mustJSON(map[string]string{"reviewStatus": "pending_review"}),             ChangedBy:  ctx.Value("userID").(string),         }).Error     } } ``` 
