> 来源：`docs/05-工作流引擎.md` 第 232 行 > 位置：[总目录](../../README.md) -> [05 - 工作流引擎](../README.md) -> 当前章节 > 相邻：[上一篇](5.3.2-转换验证.md) | [下一篇](5.3.4-依赖注入-—-回调所需的全部依赖.md) > 相关主题：[06 - 插件与钩子体系](../../06-插件与钩子体系/README.md) | [07 - AI 编程工具集成](../../07-AI编程工具集成/README.md) | [08 - Git 集成方案](../../08-Git集成方案/README.md) | [09 - 监控与反馈闭环](../../09-监控与反馈闭环/README.md) | [11 - 测试策略](../../11-测试策略/README.md) ### 5.3.3 状态持久化与恢复  ```go // internal/fsm/persistence.go  // SaveState 将 fsm 当前状态写回 Issue 记录 func (f *IssueFSM) SaveState(ctx context.Context, issue *model.Issue) error {     issue.Status = f.Current()     return f.db.WithContext(ctx).Model(issue).Update("status", issue.Status).Error }  // RestoreFSM 从数据库恢复 FSM 实例 func RestoreIssueFSM(ctx context.Context, db *gorm.DB, deps *FSMDependencies, issueID string) (*IssueFSM, *model.Issue, error) {     var issue model.Issue     if err := db.WithContext(ctx).First(&issue, "id = ?", issueID).Error; err != nil {         return nil, nil, err     }     return NewIssueFSM(&issue, deps), &issue, nil }  // Transition 执行状态转换 + 自动持久化 // 对外暴露的统一入口，业务层只需调用此方法 func (f *IssueFSM) Transition(ctx context.Context, issue *model.Issue, event string) error {     if err := f.Event(ctx, event); err != nil {         return fmt.Errorf("fsm: %w", err)     }     return f.SaveState(ctx, issue) } ``` 
