> 来源：`docs/05-工作流引擎.md` 第 93 行 > 位置：[总目录](../README.md) -> [05 - 工作流引擎](README.md) -> 当前章节 > 相邻：[上一篇](5.1-Issue-生命周期状态机.md) | [下一篇](5.3-状态机实现-—-基于-looplabfsm/README.md) > 相关主题：[06 - 插件与钩子体系](../06-插件与钩子体系/README.md) | [07 - AI 编程工具集成](../07-AI编程工具集成/README.md) | [08 - Git 集成方案](../08-Git集成方案/README.md) | [09 - 监控与反馈闭环](../09-监控与反馈闭环/README.md) | [11 - 测试策略](../11-测试策略/README.md) ## 5.2 状态详解  | 状态 | 含义 | 可触发动作 | |------|------|-----------| | `draft` | 草稿，待审核 | approve / reject / edit / delete | | `approved` | 审核通过，等待生成计划并执行 | enqueue / edit / reject | | `rejected` | 审核驳回 | reopen / edit / delete / close | | `queued` | 已进入执行队列 | cancel | | `running` | AI 正在执行 | cancel / pause | | `paused` | 暂停中 | continue / cancel | | `done` | 执行成功 | create_pr / retry / close | | `failed` | 执行失败 | retry / edit / reject / close | | `cancelled` | 已取消 | retry / close | | `pr_ready` | 代码就绪，待创建 PR | create_pr / retry | | `pr_created` | PR 已创建 | (Webhook 驱动后续状态) / reopen | | `deployed` | 已部署 | close / reopen | | `closed` | 已关闭 | reopen（重新打开后自动回到 `approved`） | | `reopened` | 已重新打开 | → approved（自动回到可执行入口） |  > **审核要求**：Issue 执行前**必须经过审核**（`draft` → `approve` → `approved` → `enqueue` → `running`）。`approved` 是进入计划生成与执行队列的唯一入口；执行计划可在审批后提前生成，也可在 Worker 真正开始执行前自动补齐。`close` 后 `reopen` 会先进入 `reopened`，再自动回到 `approved`，无需额外人工 `approve`。`fast_exec` 仅限 Admin 角色显式传 `force=true` 使用。 > > **关闭/重开循环**：Issue 在 `done`、`failed`、`cancelled`、`deployed`、`rejected` 状态下均可 `close`。关闭后随时 `reopen`，重开后自动进入 `approved`，可重新生成计划并再次入队执行。 
