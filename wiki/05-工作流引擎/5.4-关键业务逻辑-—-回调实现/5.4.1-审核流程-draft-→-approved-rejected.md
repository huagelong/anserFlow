> 来源：`docs/05-工作流引擎.md` 第 303 行 > 位置：[总目录](../../README.md) -> [05 - 工作流引擎](../README.md) -> 当前章节 > 相邻：[上一篇](README.md) | [下一篇](5.4.2-执行调度-approved-→-queued-→-running.md) > 相关主题：[06 - 插件与钩子体系](../../06-插件与钩子体系/README.md) | [07 - AI 编程工具集成](../../07-AI编程工具集成/README.md) | [08 - Git 集成方案](../../08-Git集成方案/README.md) | [09 - 监控与反馈闭环](../../09-监控与反馈闭环/README.md) | [11 - 测试策略](../../11-测试策略/README.md) ### 5.4.1 审核流程 (draft → approved / rejected)  ```go // internal/fsm/issue_callbacks.go  // beforeApprove — 审核前置守卫 func (f *IssueFSM) beforeApprove(ctx context.Context, e *fsm.Event) {     // 操作者必须是 admin     userID, _ := ctx.Value("userID").(string)     userRole, _ := ctx.Value("userRole").(string)     if userRole != "admin" {         e.Cancel(fmt.Errorf("only admin can approve"))         return     }      // 项目必须已配置 Git 凭证     project, _ := f.svc.DB.GetProject(ctx, e.Args[0].(string))  // projectID     if project.GitRepoUrl == "" {         e.Cancel(fmt.Errorf("project git URL not configured"))         return     }     e.Args = append(e.Args, project) // 传递给 after_approve }  func (f *IssueFSM) afterApprove(ctx context.Context, e *fsm.Event) {     project := e.Args[0].(*model.Project)     issue := e.Args[1].(*model.Issue)      // 自动执行：project.settings.autoExecute === true     if project.Settings.AutoExecute {         f.Event(ctx, "enqueue")  // 衔接执行     }      // 发射 Hook     f.svc.HookRegistry.Emit(ctx, "issue:approved", issue) }  // afterReject — 审核驳回 func (f *IssueFSM) afterReject(ctx context.Context, e *fsm.Event) {     issue := e.Args[0].(*model.Issue)     reason := e.Args[1].(string)  // 驳回原因      // 记录驳回原因     f.db.Model(issue).Update("reject_reason", reason)      // 发射 Hook     f.svc.HookRegistry.Emit(ctx, "issue:rejected", issue, reason) } ``` 
