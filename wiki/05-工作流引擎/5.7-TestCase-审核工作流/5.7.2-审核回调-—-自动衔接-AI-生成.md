> 来源：`docs/05-工作流引擎.md` 第 761 行 > 位置：[总目录](../../README.md) -> [05 - 工作流引擎](../README.md) -> 当前章节 > 相邻：[上一篇](5.7.1-TestCase-FSM-实现.md) | [下一篇](5.7.3-完整审核→生成→测试流程-API-层.md) > 相关主题：[06 - 插件与钩子体系](../../06-插件与钩子体系/README.md) | [07 - AI 编程工具集成](../../07-AI编程工具集成/README.md) | [08 - Git 集成方案](../../08-Git集成方案/README.md) | [09 - 监控与反馈闭环](../../09-监控与反馈闭环/README.md) | [11 - 测试策略](../../11-测试策略/README.md) ### 5.7.2 审核回调 — 自动衔接 AI 生成  ```go // internal/fsm/test_case_callbacks.go  func (f *TestCaseFSM) beforeTestCaseApprove(ctx context.Context, e *fsm.Event) {     // admin 权限守卫     role, _ := ctx.Value("userRole").(string)     if role != "admin" {         e.Cancel(fmt.Errorf("only admin can approve"))     } }  // afterTestCaseApprove — 审核通过后自动触发 AI 生成 func (f *TestCaseFSM) afterTestCaseApprove(ctx context.Context, e *fsm.Event) {     tc := e.Args[0].(*model.TestCase)      // 同步更新 reviewStatus 字段     f.db.Model(tc).Update("reviewStatus", "approved")      f.svc.HookRegistry.Emit(ctx, HookTestCaseApproved, tc)      // 自动衔接: approved → generating     go func() {         _ = f.Event(context.Background(), "generate")         // generate 触发 Asynq 任务调用 AI     }() }  // afterTestCaseReject — 审核驳回 func (f *TestCaseFSM) afterTestCaseReject(ctx context.Context, e *fsm.Event) {     tc := e.Args[0].(*model.TestCase)      // 同步更新 reviewStatus 字段     f.db.Model(tc).Update("reviewStatus", "rejected")      f.svc.HookRegistry.Emit(ctx, HookTestCaseRejected, tc) }  // afterGenerate — 投递 AI 生成任务 func (f *TestCaseFSM) afterGenerate(ctx context.Context, e *fsm.Event) {     tc := e.Args[0].(*model.TestCase)      task, _ := asynq.NewTask("test_case:generate", marshal(GenTestPayload{         TestCaseID:  tc.ID,         Description: tc.Description,         IssueID:     tc.IssueID,     }), asynq.MaxRetry(0))     f.svc.AsynqClient.Enqueue(task) }  // afterGenerated — AI 生成完成后自动运行测试 func (f *TestCaseFSM) afterGenerated(ctx context.Context, e *fsm.Event) {     tc := e.Args[0].(*model.TestCase)      f.svc.HookRegistry.Emit(ctx, HookTestCaseGenerated, tc)      // 自动运行测试（Docker 沙盒内执行）     result, err := f.svc.TestRunner.Run(ctx, tc)     if err != nil {         // runner 异常，标记失败但记录错误         f.db.Model(tc).Update("result", map[string]any{             "status": "failed",             "errors": []map[string]any{{"message": err.Error()}},             "runAt":  time.Now(),         })         f.Event(ctx, "fail")         return     }      // 写入结果     f.db.Model(tc).Updates(map[string]any{         "status": result.Status,         "result": result,     })      if result.Status == "passed" {         f.Event(ctx, "pass")     } else {         f.Event(ctx, "fail")     } }  func (f *TestCaseFSM) afterPassOrFail(ctx context.Context, e *fsm.Event) {     tc := e.Args[0].(*model.TestCase)     hookName := HookTestCasePassed     if e.Event == "fail" {         hookName = HookTestCaseFailed     }     f.svc.HookRegistry.Emit(ctx, hookName, tc) } ``` 
