> 来源：`docs/08-Git集成方案.md` 第 1263 行 > 位置：[总目录](../../README.md) -> [08 - Git 集成方案](../README.md) -> 当前章节 > 相邻：[上一篇](README.md) | [下一篇](字段映射.md) > 相关主题：[05 - 工作流引擎](../../05-工作流引擎/README.md) | [07 - AI 编程工具集成](../../07-AI编程工具集成/README.md) | [10 - 部署与运维](../../10-部署与运维/README.md) ### 平台接口方法  ```go // internal/adapter/git/provider.go  type ListIssueOptions struct {     State  string    // open / closed / all，默认 open     Since  time.Time // 只拉取此时间之后的（增量导入）     Page   int       // 分页，1-based     PerPage int      // 每页数量，默认 30，最大 100 }  type RemoteIssue struct {     Number    int       `json:"number"`     // 平台侧 Issue 编号     Title     string    `json:"title"`     Body      string    `json:"body"`       // Markdown     State     string    `json:"state"`      // open / closed     Labels    []string  `json:"labels"`     Assignee  string    `json:"assignee"`   // 用户名     Milestone string    `json:"milestone"`     URL       string    `json:"url"`        // https://github.com/.../issues/123     CreatedAt time.Time `json:"created_at"`     UpdatedAt time.Time `json:"updated_at"` } ```  `GitProvider` 主接口已包含 `ListIssues` / `GetIssue`，这里仅补充导入相关参数与返回结构。 
