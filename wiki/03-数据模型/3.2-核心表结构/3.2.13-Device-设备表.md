> 来源：`docs/03-数据模型.md` 第 692 行 > 位置：[总目录](../../README.md) -> [03 - 数据模型](../README.md) -> 当前章节 > 相邻：[上一篇](3.2.12-AuditLog-操作审计表.md) | [下一篇](3.2.14-IssueLabel-多对多关联表.md) > 相关主题：[04 - API 设计](../../04-API设计/README.md) | [05 - 工作流引擎](../../05-工作流引擎/README.md) | [09 - 监控与反馈闭环](../../09-监控与反馈闭环/README.md) ### 3.2.13 Device 设备表  记录用户登录设备，支持远程吊销和多设备管理：  ```go type Device struct {     ID         string    `gorm:"primaryKey;type:varchar(36)" json:"id"`     UserID     string    `gorm:"index;type:varchar(36)" json:"userId"`     Name       string    `gorm:"type:varchar(100)" json:"deviceName"`        // MacBook Pro M3 / CLI on Ubuntu     DeviceKey  string    `gorm:"type:varchar(64);uniqueIndex" json:"deviceKey"` // 机器特征哈希     OS         string    `gorm:"type:varchar(20)" json:"os"`             // darwin / linux / windows     Arch       string    `gorm:"type:varchar(20)" json:"arch"`            // arm64 / amd64     Source     string    `gorm:"type:varchar(20)" json:"source"`          // web / cli     IP         string    `gorm:"type:varchar(45)" json:"ip"`     UserAgent  string    `gorm:"type:varchar(500)" json:"userAgent"`     Current    bool      `gorm:"default:false" json:"current"`           // 是否当前会话     LastSeenAt time.Time `json:"lastSeenAt"`     CreatedAt  time.Time `gorm:"autoCreateTime" json:"createdAt"` } ```  ```sql CREATE TABLE devices (     id VARCHAR(36) PRIMARY KEY,     user_id VARCHAR(36) NOT NULL,     name VARCHAR(100),     device_key VARCHAR(64) NOT NULL UNIQUE,     os VARCHAR(20),                            -- darwin / linux / windows     arch VARCHAR(20),                           -- arm64 / amd64     source VARCHAR(20) DEFAULT 'web',     ip VARCHAR(45) NOT NULL,     user_agent VARCHAR(500),     current BOOLEAN DEFAULT FALSE,     last_seen_at DATETIME,     created_at DATETIME NOT NULL,     INDEX idx_device_user (user_id, current) ); ``` 
