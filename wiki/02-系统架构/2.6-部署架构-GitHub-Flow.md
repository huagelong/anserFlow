> 来源：`docs/02-系统架构.md` 第 1209 行 > 位置：[总目录](../README.md) -> [02 - 系统架构](README.md) -> 当前章节 > 相邻：[上一篇](2.5-数据流/监控反馈流程：线上问题-→-自动Issue.md) | 下一篇：无 > 相关主题：[03 - 数据模型](../03-数据模型/README.md) | [04 - API 设计](../04-API设计/README.md) | [05 - 工作流引擎](../05-工作流引擎/README.md) | [10 - 部署与运维](../10-部署与运维/README.md) ## 2.6 部署架构 (GitHub Flow)  ```                         ┌─────────────────────────┐                         │        Nginx             │                         │  (反向代理 + 静态资源)     │                         │  HTTP/2 + HTTPS           │                         └─────────────┬───────────┘                                       │                     ┌─────────────────┴─────────────────┐                     │                                   │            ┌────────┴────────┐                  ┌───────┴──────────┐            │   Web 容器       │                  │  Server 容器      │            │  (Nginx + SPA)  │                  │  (Go 二进制)      │            │  Vue 3 静态文件  │                  │  Gin API 服务     │            └─────────────────┘                  └───────┬──────────┘                                                         │                           ┌─────────────────────────────┼────────────────┐                           │                             │                │                    ┌──────┴──────┐               ┌──────┴──────┐  ┌──────┴──────┐                    │   MySQL 8   │               │   Redis 7   │  │  Docker 沙盒  │                    │  (主数据库)  │               │ (缓存+队列)  │  │    dockerd   │                    └─────────────┘               └─────────────┘  └─────────────┘ ```  **GitHub Flow 分支策略**： - `main` 分支始终保持可部署状态 - 功能开发从 `main` 拉出 `flowcode/<issue-id>-<slug>` 分支 - 通过 PR 合并回 `main`，触发 GitHub Actions 自动构建 Docker 镜像 - 所有服务通过 `docker-compose.yml` 编排，单机即可运行。最低 1C2G 云服务器。
