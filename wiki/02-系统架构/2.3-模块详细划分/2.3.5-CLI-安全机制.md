> 来源：`docs/02-系统架构.md` 第 402 行 > 位置：[总目录](../../README.md) -> [02 - 系统架构](../README.md) -> 当前章节 > 相邻：[上一篇](2.3.4-Skills-目录约定.md) | [下一篇](2.3.6-WebSocket-分布式架构.md) > 相关主题：[03 - 数据模型](../../03-数据模型/README.md) | [04 - API 设计](../../04-API设计/README.md) | [05 - 工作流引擎](../../05-工作流引擎/README.md) | [10 - 部署与运维](../../10-部署与运维/README.md) ### 2.3.5 CLI 安全机制  CLI 工具直接操作云端的 Issue、触发执行、安装 Skill，安全性至关重要。  #### 2.3.5a Token 安全存储  ```go // internal/cli/auth/store.go  // 凭证按 OS 标准路径加密存储 // Linux:   ~/.config/flowcode/auth.json (权限 0600) // macOS:   ~/Library/Application Support/flowcode/auth.json // Windows: %APPDATA%/flowcode/auth.json  type CredentialStore struct {     path string     key  []byte // 从机器特征派生 (hostname + machine-id + os) }  func (s *CredentialStore) Save(cred *Credentials) error {     data, _ := json.Marshal(cred)     // AES-256-GCM 加密后写入文件     encrypted := aesGCMEncrypt(data, s.key)     return os.WriteFile(s.path, encrypted, 0600) }  func (s *CredentialStore) Load() (*Credentials, error) {     encrypted, err := os.ReadFile(s.path)     if err != nil {         return nil, fmt.Errorf("not logged in, run 'flowcode login' first")     }     return aesGCMDecrypt(encrypted, s.key) }  type Credentials struct {     AccessToken  string    `json:"access_token"`     RefreshToken string    `json:"refresh_token"`     ServerURL    string    `json:"server_url"`     ExpiresAt    time.Time `json:"expires_at"`     DeviceID     string    `json:"device_id"` } ```  #### 2.3.5b 设备注册与绑定  首次登录时，CLI 向服务端注册设备，后续操作可被审计追溯到具体设备：  ```go // internal/cli/auth/device.go  type DeviceInfo struct {     DeviceID   string // SHA256(hostname + machine-id)     DeviceName string // "MacBook Pro" / "CI Runner"     OS         string // "darwin" / "linux" / "windows"     Arch       string // "arm64" / "amd64" }  func RegisterDevice(client *api.Client, device DeviceInfo) error {     return client.Post("/api/v1/devices", device) } ```  服务端记录设备列表，用户可在 Web 控制台查看并远程吊销已登录设备。  #### 2.3.5c 自动 Token 刷新  CLI 在请求前自动检查 Token 过期时间，透明刷新：  ```go // internal/cli/client/interceptor.go  func (c *Client) Do(req *http.Request) (*http.Response, error) {     cred, _ := c.store.Load()      // Token 将在 5 分钟内过期 → 提前刷新     if time.Until(cred.ExpiresAt) < 5*time.Minute {         newCred, err := c.refreshToken(cred.RefreshToken)         if err != nil {             return nil, fmt.Errorf("session expired, run 'flowcode login'")         }         c.store.Save(newCred)         cred = newCred     }      req.Header.Set("Authorization", "Bearer "+cred.AccessToken)     req.Header.Set("X-Device-ID", cred.DeviceID)     return c.http.Do(req) } ```  #### 2.3.5d CLI 操作审计  所有 CLI 发起的敏感操作记录在服务端的 `audit_logs` 表中（source=`cli`），登录事件记录在 `login_logs` 表：  ```bash # 用户可在 Web 控制台查看 CLI 操作历史 + 登录记录 flowcode audit-log --since 7d ```  #### 2.3.5e CLI 安全最佳实践  | 措施 | 说明 | |------|------| | 本地凭证加密 | 使用 AES-256-GCM + 机器特征派生密钥，离开本机无法解密 | | 文件权限 | 凭证文件 chmod 0600，仅当前用户可读写 | | Token 过期 | Access Token 2h，Refresh Token 30d 滚动刷新 | | 设备注册 | 每次登录注册设备，Web 端可远程吊销 | | 操作签名 | 敏感操作 (execute/skill install) 附带 HMAC 签名 | | 审计日志 | 所有 CLI 操作记录到服务端审计表，可追溯 | | 输入消毒 | CLI 参数在发送前做 trim + 转义处理 | 
