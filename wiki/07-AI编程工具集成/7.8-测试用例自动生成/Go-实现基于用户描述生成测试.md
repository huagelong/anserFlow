> 来源：`docs/07-AI编程工具集成.md` 第 1423 行 > 位置：[总目录](../../README.md) -> [07 - AI 编程工具集成](../README.md) -> 当前章节 > 相邻：[上一篇](语言-框架映射表.md) | 下一篇：无 > 相关主题：[05 - 工作流引擎](../../05-工作流引擎/README.md) | [08 - Git 集成方案](../../08-Git集成方案/README.md) | [10 - 部署与运维](../../10-部署与运维/README.md) | [11 - 测试策略](../../11-测试策略/README.md) ### Go 实现（基于用户描述生成测试）  ```go // internal/service/test_runner.go  type TestRunner struct {     db       *gorm.DB     aiTool   adapter.AIToolAdapter     sandbox  *SandboxManager }  // Run 在 Docker 沙盒中执行测试代码，返回结构化结果 // 优先级: tc.TestScript (用户自定义) > framework 默认映射 func (r *TestRunner) Run(ctx context.Context, tc *model.TestCase) (*TestResult, error) {     issue, _ := r.getIssue(ctx, tc.IssueID)     project, _ := r.getProject(ctx, issue.ProjectID)      // 1. 创建 Docker 沙盒（项目级可覆盖镜像，见 config/sandbox.yaml overrides）     sb, err := r.sandbox.Create(ctx, project.OrgID+"/test-"+tc.ID)     if err != nil {         return nil, fmt.Errorf("create sandbox: %w", err)     }     defer r.sandbox.Cleanup(ctx, sb)      // 2. 拉取项目代码     if err := r.sandbox.PrepareWorkspace(ctx, sb, project); err != nil {         return nil, fmt.Errorf("prepare workspace: %w", err)     }      // 3. 注入测试代码到容器内正确位置（先注入，{testFile} 变量依赖此路径）     testPath := fmt.Sprintf("/workspace/repo/%s", tc.TestFilePath)     writeCmd := []string{"sh", "-c", fmt.Sprintf(         "cat > %s << 'EOTEST'\n%s\nEOTEST", testPath, tc.TestCode,     )}     if _, err := r.sandbox.Exec(ctx, sb, writeCmd, nil); err != nil {         return nil, fmt.Errorf("inject test code: %w", err)     }      // 4. 解析脚本：custom testScript > framework 默认映射     script := r.resolveScript(tc, testPath)      // 5. 安装依赖     if script.Install != "" {         r.sandbox.Exec(ctx, sb, []string{"sh", "-c", script.Install}, nil)     } else if depsCmd := r.getDepsInstallCmd(tc.Framework); depsCmd != nil {         r.sandbox.Exec(ctx, sb, depsCmd, nil)     }      // 6. 执行测试命令     cmd := []string{"sh", "-c", script.Command}     result, err := r.sandbox.Exec(ctx, sb, cmd, nil)     if err != nil {         return nil, fmt.Errorf("run test: %w", err)     }      // 7. 解析结构化结果     return r.parseResult(result.Output, tc.Framework), nil }  // resolveScript 返回最终执行的脚本，custom > framework 默认 func (r *TestRunner) resolveScript(tc *model.TestCase, testPath string) Script {     s := Script{         Command: fmt.Sprintf("cd /workspace/repo && %s 2>&1", tc.Framework),     }      // 1. 用户自定义优先     if tc.TestScript != nil && tc.TestScript.Command != "" {         s.Command = tc.TestScript.Command         if tc.TestScript.Install != "" {             s.Install = tc.TestScript.Install         }         if tc.TestScript.Timeout > 0 {             s.Timeout = tc.TestScript.Timeout         }     } else {         // 2. 回退到 framework 默认映射         cmds := r.buildTestCmd(tc.Framework)         if len(cmds) > 2 {             s.Command = cmds[2] // 取 sh -c 后面的命令         }         if deps := r.getDepsInstallCmd(tc.Framework); deps != nil && len(deps) > 2 {             s.Install = deps[2]         }     }      // 3. 变量替换     s.Command = strings.NewReplacer(         "{testFile}", testPath,         "{workspace}", "/workspace/repo",         "{projectDir}", filepath.Base(testPath[0:strings.LastIndex(testPath, "/")]),     ).Replace(s.Command)     s.Install = strings.NewReplacer(         "{testFile}", testPath,         "{workspace}", "/workspace/repo",         "{projectDir}", filepath.Base(testPath[0:strings.LastIndex(testPath, "/")]),     ).Replace(s.Install)      // 4. 确保 stderr 重定向到 stdout     if !strings.Contains(s.Command, "2>&1") {         s.Command += " 2>&1"     }      return s }  type Script struct {     Install string     Command string     Timeout int }  // ── 语言/框架 → 依赖安装 + 测试命令映射 ──  func (r *TestRunner) getDepsInstallCmd(framework string) []string {     switch framework {     case "testing":         return []string{"sh", "-c", "cd /workspace/repo && go mod download 2>/dev/null; true"}     case "vitest", "jest", "mocha":         return []string{"sh", "-c", "cd /workspace/repo && npm install 2>/dev/null; true"}     case "pytest", "unittest":         return []string{"sh", "-c", "cd /workspace/repo && pip install -r requirements.txt 2>/dev/null; true"}     case "maven":         return []string{"sh", "-c", "cd /workspace/repo && mvn dependency:resolve -q 2>/dev/null; true"}     case "dotnet":         return []string{"sh", "-c", "cd /workspace/repo && dotnet restore 2>/dev/null; true"}     default:         return nil // gradle / cargo / 其他不需要显式安装     } }  func (r *TestRunner) buildTestCmd(framework string) []string {     switch framework {     // Go     case "testing":         return []string{"sh", "-c", "cd /workspace/repo && go test ./... -v -cover 2>&1"}      // TypeScript / JavaScript     case "vitest":         return []string{"sh", "-c", "cd /workspace/repo && npx vitest run --reporter=verbose 2>&1"}     case "jest":         return []string{"sh", "-c", "cd /workspace/repo && npx jest --verbose 2>&1"}     case "mocha":         return []string{"sh", "-c", "cd /workspace/repo && npx mocha --reporter=spec 2>&1"}      // Python     case "pytest":         return []string{"sh", "-c", "cd /workspace/repo && python -m pytest -v --tb=short 2>&1"}     case "unittest":         return []string{"sh", "-c", "cd /workspace/repo && python -m unittest discover -v 2>&1"}      // Java     case "maven":         return []string{"sh", "-c", "cd /workspace/repo && mvn test -B 2>&1"}     case "gradle":         return []string{"sh", "-c", "cd /workspace/repo && ./gradlew test --console=plain 2>&1"}      // Rust     case "cargo":         return []string{"sh", "-c", "cd /workspace/repo && cargo test --verbose 2>&1"}      // .NET     case "dotnet":         return []string{"sh", "-c", "cd /workspace/repo && dotnet test --verbosity normal 2>&1"}      default:         return []string{"sh", "-c", fmt.Sprintf("cd /workspace/repo && %s 2>&1", framework)}     } }  func (r *TestRunner) parseResult(output string, framework string) *TestResult {     tr := &TestResult{         Output: output,         RunAt:  time.Now(),     }      // 通用解析：匹配 PASS/FAIL 行和覆盖率     passRe := regexp.MustCompile(`--- PASS:`)     failRe := regexp.MustCompile(`--- FAIL:`)     covRe := regexp.MustCompile(`coverage: ([\d.]+)%`)      tr.Passed = len(passRe.FindAllString(output, -1))     tr.Failed = len(failRe.FindAllString(output, -1))     tr.Total = tr.Passed + tr.Failed      if matches := covRe.FindStringSubmatch(output); len(matches) > 1 {         tr.Coverage = matches[1] + "%"     }      if tr.Failed == 0 && tr.Total > 0 {         tr.Status = "passed"     } else {         tr.Status = "failed"         // 提取失败详情         tr.Errors = r.extractFailureDetails(output)     }      return tr }  func (r *TestRunner) extractFailureDetails(output string) []TestError {     re := regexp.MustCompile(`(\S+):(\d+):\s*(.+)`)     var errors []TestError     for _, match := range re.FindAllStringSubmatch(output, -1) {         line, _ := strconv.Atoi(match[2])         errors = append(errors, TestError{             File:    match[1],             Line:    line,             Message: match[3],         })     }     return errors }  // TestResult + TestError 定义见 03-数据模型.md 中 `TestCase.result` 与 `TestResult` 结构说明 ```
