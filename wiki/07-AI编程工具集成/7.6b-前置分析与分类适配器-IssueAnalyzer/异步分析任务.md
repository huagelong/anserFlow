> 来源：`docs/07-AI编程工具集成.md` 第 1115 行 > 位置：[总目录](../../README.md) -> [07 - AI 编程工具集成](../README.md) -> 当前章节 > 相邻：[上一篇](分析-Prompt-模板.md) | [下一篇](分析与计划触发时机.md) > 相关主题：[05 - 工作流引擎](../../05-工作流引擎/README.md) | [08 - Git 集成方案](../../08-Git集成方案/README.md) | [10 - 部署与运维](../../10-部署与运维/README.md) | [11 - 测试策略](../../11-测试策略/README.md) ### 异步分析任务  ```go // internal/queue/analyze_handler.go  func HandleAnalyzeTask(ctx context.Context, t *asynq.Task) error {     var payload AnalyzePayload     json.Unmarshal(t.Payload(), &payload)      // 1. 加载 Issue     var issue model.Issue     db.First(&issue, "id = ?", payload.IssueID)      // 2. 加载项目（获取上下文）     var project model.Project     db.First(&project, "id = ?", issue.ProjectID)      // 3. 调用 IssueAnalyzer（单次调用完成分析+分类）     analyzer := &IssueAnalyzer{opencodePath: "opencode"}     result, err := analyzer.Analyze(ctx, AnalyzeInput{         Title:       issue.Title,         Description: issue.Description,         ProjectName: project.Name,     })     if err != nil {         slog.Warn("AI analyze failed, keeping defaults", "issue", issue.ID, "err", err)         return nil     }      // 4. 回写分析结果（isBug + needsCodeChange）     db.Model(&issue).Updates(map[string]any{         "is_bug":            result.IsBug,         "needs_code_change": result.NeedsCodeChange,         "analysis_reason":   result.AnalysisReason,         "analysis_completed": true,         "category":           result.Category,         "category_source":    "ai",         "category_confidence":  result.Confidence,         "category_classified_at": time.Now(),     })      // 5. 发射事件     hookRegistry.Emit(ctx, "issue:analyzed", map[string]any{         "issue_id":         issue.ID,         "is_bug":           result.IsBug,         "needs_code_change": result.NeedsCodeChange,         "category":         result.Category,         "confidence":       result.Confidence,     })      // 6. 若 needsCodeChange=false，标记 Issue 跳过代码执行     if !result.NeedsCodeChange {         slog.Info("Issue does not need code change, skipping execution",             "issue", issue.ID, "analysis", result.AnalysisReason)     }      return nil } ``` 
