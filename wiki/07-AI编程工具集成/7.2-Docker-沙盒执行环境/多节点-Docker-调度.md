> 来源：`docs/07-AI编程工具集成.md` 第 534 行 > 位置：[总目录](../../README.md) -> [07 - AI 编程工具集成](../README.md) -> 当前章节 > 相邻：[上一篇](配置.md) | [下一篇](与-Git-Clone-的衔接.md) > 相关主题：[05 - 工作流引擎](../../05-工作流引擎/README.md) | [08 - Git 集成方案](../../08-Git集成方案/README.md) | [10 - 部署与运维](../../10-部署与运维/README.md) | [11 - 测试策略](../../11-测试策略/README.md) ### 多节点 Docker 调度  当单机资源不足，在 `config/sandbox.yaml` 的 `nodes` 中添加远程节点即可，无需改代码：  ```yaml nodes:   - "unix:///var/run/docker.sock"   - "tcp://sandbox-node1:2375"   - "tcp://sandbox-node2:2375" ```  ```go // 三种调度策略 func newScheduler(strategy string) Scheduler {     switch strategy {     case "round_robin":         return &RoundRobinScheduler{next: 0}     case "least_containers":         return &LeastContainersScheduler{}   // 默认：选容器数最少的节点     default:         return &RandomScheduler{}            // 随机     } }  // 最少容器数策略 func (s *LeastContainersScheduler) Select(clients []*client.Client) *client.Client {     var best *client.Client     minCount := int(^uint(0) >> 1)     for _, cli := range clients {         info, _ := cli.Info(context.Background())         if info.ContainersRunning < minCount {             minCount = info.ContainersRunning             best = cli         }     }     return best } ``` 
