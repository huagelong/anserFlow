> 来源：`docs/07-AI编程工具集成.md` 第 573 行 > 位置：[总目录](../../README.md) -> [07 - AI 编程工具集成](../README.md) -> 当前章节 > 相邻：[上一篇](多节点-Docker-调度.md) | [下一篇](多节点-Docker-调度分布式可选.md) > 相关主题：[05 - 工作流引擎](../../05-工作流引擎/README.md) | [08 - Git 集成方案](../../08-Git集成方案/README.md) | [10 - 部署与运维](../../10-部署与运维/README.md) | [11 - 测试策略](../../11-测试策略/README.md) ### 与 Git Clone 的衔接  ```go func (d *DockerSandbox) PrepareWorkspace(ctx context.Context, sb *Sandbox, project *model.Project) error {     // 在容器内 clone 代码     cloneCmd := []string{"git", "clone", "--depth", "1", project.GitRepoUrl, "/workspace/repo"}     if _, err := d.Exec(ctx, sb, cloneCmd, nil); err != nil {         return fmt.Errorf("clone in sandbox: %w", err)     }      // 注入 Skills + 分析摘要 + 执行计划     skillsContent := d.loadSkills(ctx, project.ID)     writeFileCmd := fmt.Sprintf("echo '%s' > /workspace/.flowcode_prompt.md", skillsContent)     d.Exec(ctx, sb, []string{"sh", "-c", writeFileCmd}, nil)      return nil }  // 销毁沙盒前提交变更 func (d *DockerSandbox) CommitAndPush(ctx context.Context, sb *Sandbox, issue *model.Issue) (string, error) {     cmds := [][]string{         {"git", "-C", "/workspace/repo", "add", "-A"},         {"git", "-C", "/workspace/repo", "commit", "-m", issue.Title},         {"git", "-C", "/workspace/repo", "push", "origin", issue.GitBranch},     }     var lastSHA string     for _, cmd := range cmds {         result, err := d.Exec(ctx, sb, cmd, nil)         if err != nil {             return "", err         }         if strings.Contains(strings.Join(cmd, " "), "push") {             lastSHA = extractSHA(result.Output)         }     }     return lastSHA, nil } ``` 
