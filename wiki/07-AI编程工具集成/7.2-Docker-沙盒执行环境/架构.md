> 来源：`docs/07-AI编程工具集成.md` 第 178 行 > 位置：[总目录](../../README.md) -> [07 - AI 编程工具集成](../README.md) -> 当前章节 > 相邻：[上一篇](README.md) | [下一篇](Docker-沙盒镜像.md) > 相关主题：[05 - 工作流引擎](../../05-工作流引擎/README.md) | [08 - Git 集成方案](../../08-Git集成方案/README.md) | [10 - 部署与运维](../../10-部署与运维/README.md) | [11 - 测试策略](../../11-测试策略/README.md) ### 架构  ``` ┌─────────────────────────────────────────────────────────┐ │                   flowcode Server (Go)                  │ │  ┌───────────────────────────────────────────────────┐  │ │  │              SandboxManager                       │  │ │  │                                                  │  │ │  │  ┌────────────────┐  ┌────────────────┐          │  │ │  │  │ Container A    │  │ Container B    │  ...     │  │ │  │  │ (issue-abc123) │  │ (issue-def456) │          │  │ │  │  │                │  │                │          │  │ │  │  │ flowcode/      │  │ flowcode/      │          │  │ │  │  │ sandbox:latest │  │ sandbox:latest │          │  │ │  │  └────────────────┘  └────────────────┘          │  │ │  └──────────────┬────────────────────────────────────┘  │ │                 │ Docker SDK (containerd)               │ └─────────────────┼──────────────────────────────────────┘                   │          ┌────────┴────────┐          │    dockerd      │          │  (Unix Socket)  │          └─────────────────┘ ```  每个 Issue 执行时，`SandboxManager`：  1. 拉取/复用 `flowcode/sandbox:latest` 镜像（基础镜像至少包含 Git + Node/Python/Go；当前仓库默认预装 `opencode`，`codex` / `claude-code` 可按部署环境继续扩展） 2. 创建容器，挂载独立 volume `flowcode-sandbox-{issueId}` 作为 `/workspace` 3. 容器内 `git clone` 目标仓库到 `/workspace/repo` 4. 注入 Skills、分析摘要、执行计划到 `/workspace/.flowcode_prompt.md` 5. 容器内通过 CLI 调用 AI 工具（`opencode run --workspace /workspace/repo ...`） 6. 实时捕获 stdout/stderr → WebSocket 推送 7. 执行完毕：提交变更 → push → 销毁容器（`KeepOnError` 时保留现场） 
