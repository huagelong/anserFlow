> 来源：`docs/07-AI编程工具集成.md` 第 850 行 > 位置：[总目录](../../README.md) -> [07 - AI 编程工具集成](../README.md) -> 当前章节 > 相邻：[上一篇](工具概述.md) | [下一篇](配置项.md) > 相关主题：[05 - 工作流引擎](../../05-工作流引擎/README.md) | [08 - Git 集成方案](../../08-Git集成方案/README.md) | [10 - 部署与运维](../../10-部署与运维/README.md) | [11 - 测试策略](../../11-测试策略/README.md) ### 适配方式  ```go // internal/adapter/ai_tool/claude_code.go  type ClaudeCodeAdapter struct {     BaseAIToolAdapterImpl     sandbox *SandboxManager }  func (a *ClaudeCodeAdapter) Execute(ctx ExecuteContext) (*ExecuteResult, error) {     startTime := time.Now()      // 准备 prompt 文件（ctx.Prompt 已含分析摘要和执行计划）     promptFile := filepath.Join(ctx.WorkspaceDir, ".flowcode_prompt.md")     os.WriteFile(promptFile, []byte(ctx.Prompt), 0644)      // 通过本地进程调用 claude CLI     cmdCtx, cancel := context.WithTimeout(ctx.Ctx, 15*time.Minute)     defer cancel()      model := getString(ctx.Config, "model", "claude-sonnet-4-20250514")     endpoint := getString(ctx.Config, "endpoint", "https://api.anthropic.com")     apiKey := getString(ctx.Config, "apiKey", "")     maxTurns := getString(ctx.Config, "maxTurns", "60")      cmd := exec.CommandContext(cmdCtx, "claude",         "--prompt-file", promptFile,         "--model", model,         "--max-turns", maxTurns,         "--output-format", "stream-json",         "--dangerously-skip-permissions",         "--verbose",     )     cmd.Dir = ctx.WorkspaceDir     cmd.Env = append(os.Environ(),         "ANTHROPIC_API_KEY="+apiKey,         "ANTHROPIC_BASE_URL="+endpoint,     )      // 流式收集日志     stdoutPipe, _ := cmd.StdoutPipe()     stderrPipe, _ := cmd.StderrPipe()      var stdout, stderr strings.Builder      go scanStreamJSON(stdoutPipe, func(line []byte) {         var msg map[string]any         if err := json.Unmarshal(line, &msg); err != nil {             ctx.OnLog(LogEntry{Timestamp: time.Now().UnixMilli(), Level: "debug", Content: string(line)})             return         }         stdout.Write(line)         stdout.WriteString("\n")          switch msg["type"] {         case "stream_event":             if event, ok := msg["event"].(map[string]any); ok {                 if cb, ok := event["content_block"].(map[string]any); ok {                     if text, ok := cb["text"].(string); ok {                         ctx.OnLog(LogEntry{Timestamp: time.Now().UnixMilli(), Level: "info", Content: text})                     }                 }             }         case "tool_result":             ctx.OnLog(LogEntry{Timestamp: time.Now().UnixMilli(), Level: "info",                 Content: fmt.Sprintf("[Tool Result] %v: OK", msg["tool_name"]),                 Step:    fmt.Sprint(msg["tool_name"])})         case "error":             ctx.OnLog(LogEntry{Timestamp: time.Now().UnixMilli(), Level: "error", Content: fmt.Sprint(msg["message"])})         }     })      go scanStreamText(stderrPipe, func(line string) {         ctx.OnLog(LogEntry{Timestamp: time.Now().UnixMilli(), Level: "warn", Content: line})         stderr.WriteString(line + "\n")     })      // 执行     err := cmd.Start()     if err != nil {         return nil, fmt.Errorf("start claude: %w", err)     }     err = cmd.Wait()      exitCode := 0     if err != nil {         if exitErr, ok := err.(*exec.ExitError); ok {             exitCode = exitErr.ExitCode()         }     }      filesChanged := getGitChangedFiles(ctx.WorkspaceDir)      return &ExecuteResult{         Success:      exitCode == 0,         ExitCode:     exitCode,         FilesChanged: filesChanged,         Stdout:       stdout.String(),         Stderr:       stderr.String(),         Duration:     time.Since(startTime).Milliseconds(),     }, nil } ``` 
