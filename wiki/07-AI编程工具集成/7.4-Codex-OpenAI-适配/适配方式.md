> 来源：`docs/07-AI编程工具集成.md` 第 729 行 > 位置：[总目录](../../README.md) -> [07 - AI 编程工具集成](../README.md) -> 当前章节 > 相邻：[上一篇](工具概述.md) | [下一篇](配置项.md) > 相关主题：[05 - 工作流引擎](../../05-工作流引擎/README.md) | [08 - Git 集成方案](../../08-Git集成方案/README.md) | [10 - 部署与运维](../../10-部署与运维/README.md) | [11 - 测试策略](../../11-测试策略/README.md) ### 适配方式  ```go // internal/adapter/ai_tool/codex.go  type CodexAdapter struct {     BaseAIToolAdapterImpl     sandbox *SandboxManager }  func (a *CodexAdapter) Execute(ctx ExecuteContext) (*ExecuteResult, error) {     startTime := time.Now()      // 准备 prompt 文件（ctx.Prompt 已含分析摘要和执行计划）     promptFile := filepath.Join(ctx.WorkspaceDir, ".flowcode_prompt.md")     os.WriteFile(promptFile, []byte(ctx.Prompt), 0644)      // 通过本地进程调用 codex CLI     cmdCtx, cancel := context.WithTimeout(ctx.Ctx, 10*time.Minute)     defer cancel()      model := getString(ctx.Config, "model", "gpt-5")     endpoint := getString(ctx.Config, "endpoint", "https://api.openai.com/v1")     apiKey := getString(ctx.Config, "apiKey", "")      cmd := exec.CommandContext(cmdCtx, "codex",         "exec",         "--prompt-file", promptFile,         "--model", model,         "--approval-mode", "auto",         "--output-format", "stream-json",     )     cmd.Dir = ctx.WorkspaceDir     cmd.Env = append(os.Environ(),         "OPENAI_API_KEY="+apiKey,         "OPENAI_BASE_URL="+endpoint,     )      // 流式收集日志     stdoutPipe, _ := cmd.StdoutPipe()     stderrPipe, _ := cmd.StderrPipe()      var stdout, stderr strings.Builder     var lastProgress int     var mu sync.Mutex      go scanStreamJSON(stdoutPipe, func(line []byte) {         mu.Lock()         defer mu.Unlock()         var msg map[string]any         if err := json.Unmarshal(line, &msg); err != nil {             ctx.OnLog(LogEntry{Timestamp: time.Now().UnixMilli(), Level: "debug", Content: string(line)})             return         }         stdout.Write(line)         stdout.WriteString("\n")          switch msg["type"] {         case "assistant":             content := msg["message"].(map[string]any)["content"].(string)             ctx.OnLog(LogEntry{Timestamp: time.Now().UnixMilli(), Level: "info", Content: content})         case "tool_use":             ctx.OnLog(LogEntry{Timestamp: time.Now().UnixMilli(), Level: "info",                 Content: fmt.Sprintf("[Tool] %v: %v", msg["name"], msg["input"]),                 Step:    fmt.Sprint(msg["name"])})         case "progress":             pct := int(msg["percent"].(float64))             lastProgress = pct             ctx.OnProgress(pct, fmt.Sprint(msg["detail"]))         }     })      go scanStreamText(stderrPipe, func(line string) {         ctx.OnLog(LogEntry{Timestamp: time.Now().UnixMilli(), Level: "warn", Content: line})         stderr.WriteString(line + "\n")     })      // 执行     err := cmd.Start()     if err != nil {         return nil, fmt.Errorf("start codex: %w", err)     }     err = cmd.Wait()      exitCode := 0     if err != nil {         if exitErr, ok := err.(*exec.ExitError); ok {             exitCode = exitErr.ExitCode()         }     }      filesChanged := getGitChangedFiles(ctx.WorkspaceDir)      return &ExecuteResult{         Success:      exitCode == 0,         ExitCode:     exitCode,         FilesChanged: filesChanged,         Stdout:       stdout.String(),         Stderr:       stderr.String(),         Duration:     time.Since(startTime).Milliseconds(),         Metadata:     map[string]any{"lastProgress": lastProgress},     }, nil } ``` 
