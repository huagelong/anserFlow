> 来源：`docs/07-AI编程工具集成.md` 第 643 行 > 位置：[总目录](../../README.md) -> [07 - AI 编程工具集成](../README.md) -> 当前章节 > 相邻：[上一篇](工具概述.md) | [下一篇](配置项.md) > 相关主题：[05 - 工作流引擎](../../05-工作流引擎/README.md) | [08 - Git 集成方案](../../08-Git集成方案/README.md) | [10 - 部署与运维](../../10-部署与运维/README.md) | [11 - 测试策略](../../11-测试策略/README.md) ### 适配方式  ```go // internal/adapter/ai_tool/opencode.go  type OpenCodeAdapter struct {     BaseAIToolAdapterImpl     sandbox *SandboxManager }  func (a *OpenCodeAdapter) Execute(ctx ExecuteContext) (*ExecuteResult, error) {     startTime := time.Now()          // 1. 准备 prompt 文件 (注入 Skills；ctx.Prompt 已含分析摘要和执行计划)     promptContent := buildPromptWithSkills(ctx.Prompt, ctx.ProjectCtx.SkillContents)     promptFile := filepath.Join(ctx.WorkspaceDir, ".flowcode_prompt.md")     os.WriteFile(promptFile, []byte(promptContent), 0644)      // 2. 通过本地进程调用 opencode CLI     cmdCtx, cancel := context.WithTimeout(ctx.Ctx, 10*time.Minute)     defer cancel()      cmd := exec.CommandContext(cmdCtx, "opencode",         "run",         "--prompt-file", promptFile,         "--workspace", ctx.WorkspaceDir,         "--model", getString(ctx.Config, "model", "claude-sonnet-4-20250514"),         "--max-turns", "50",     )     cmd.Dir = ctx.WorkspaceDir      // 3. 流式收集日志     stdoutPipe, _ := cmd.StdoutPipe()     stderrPipe, _ := cmd.StderrPipe()          var stdout, stderr strings.Builder     go streamLogs(stdoutPipe, &stdout, ctx.OnLog, "info")     go streamLogs(stderrPipe, &stderr, func(e LogEntry) {         e.Level = "warn"         ctx.OnLog(e)     }, "warn")      // 4. 执行     err := cmd.Start()     if err != nil {         return nil, fmt.Errorf("start opencode: %w", err)     }     err = cmd.Wait()      exitCode := 0     if err != nil {         if exitErr, ok := err.(*exec.ExitError); ok {             exitCode = exitErr.ExitCode()         }     }      // 5. 收集变更文件列表     filesChanged := getGitChangedFiles(ctx.WorkspaceDir)      return &ExecuteResult{         Success:      exitCode == 0,         ExitCode:     exitCode,         FilesChanged: filesChanged,         Stdout:       stdout.String(),         Stderr:       stderr.String(),         Duration:     time.Since(startTime).Milliseconds(),     }, nil } ``` 
