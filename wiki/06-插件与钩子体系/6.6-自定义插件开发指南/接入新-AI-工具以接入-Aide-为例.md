> 来源：`docs/06-插件与钩子体系.md` 第 305 行 > 位置：[总目录](../../README.md) -> [06 - 插件与钩子体系](../README.md) -> 当前章节 > 相邻：[上一篇](README.md) | [下一篇](接入新-Git-平台以接入-Gitea-为例.md) > 相关主题：[07 - AI 编程工具集成](../../07-AI编程工具集成/README.md) | [08 - Git 集成方案](../../08-Git集成方案/README.md) | [04 - API 设计](../../04-API设计/README.md) ### 接入新 AI 工具（以接入 Aide 为例）  1. 创建 `internal/adapter/ai_tool/aide.go` 2. 实现 `AIToolAdapter` 接口的所有方法 3. 在 adapter 注册表中添加映射 4. 在 `ai_tool_configs` 表中添加对应配置 5. 前端 Setting 页面自动出现新工具选项  ```go // internal/adapter/ai_tool/aide.go  type AideAdapter struct {     BaseAIToolAdapterImpl }  func NewAideAdapter() *AideAdapter {     return &AideAdapter{         BaseAIToolAdapterImpl: BaseAIToolAdapterImpl{             cfg: AIToolConfig{                 Name:        "aide",                 DisplayName: "Aide",                 Description: "Aide AI coding assistant",             },         },     } }  func (a *AideAdapter) ValidateConfig(cfg map[string]any) error { return nil }  func (a *AideAdapter) Execute(ctx ExecuteContext) (*ExecuteResult, error) {     // 1. 准备 Docker 沙盒环境     // 2. 克隆仓库到 WorkspaceDir     // 3. 加载 Skills 内容到 prompt 前缀     // 4. 按执行计划调用 aide CLI 执行编码     // 5. 通过 ctx.OnLog 实时推送日志     startTime := time.Now()     // ... 执行逻辑     return &ExecuteResult{Success: true, Duration: time.Since(startTime).Milliseconds()}, nil }  func (a *AideAdapter) HealthCheck(cfg map[string]any) (bool, int64, error) {     return true, 120, nil }  func (a *AideAdapter) ParseCommitMessage(r *ExecuteResult) string {     return extractCommitMessage(r.Stdout) } ``` 
