> 来源：`docs/06-插件与钩子体系.md` 第 631 行 > 位置：[总目录](../README.md) -> [06 - 插件与钩子体系](README.md) -> 当前章节 > 相邻：[上一篇](6.8-Skills-技能体系/Skill-安全机制.md) | 下一篇：无 > 相关主题：[07 - AI 编程工具集成](../07-AI编程工具集成/README.md) | [08 - Git 集成方案](../08-Git集成方案/README.md) | [04 - API 设计](../04-API设计/README.md) ## 6.9 安全钩子中间件  所有插件执行前经过统一安全检查：  ```go // internal/service/hook_guard.go  type HookSecurityGuard struct {     blockedURLs []string        // 禁止的目标 URL (内网地址等)     maxWebhookBatch int         // 最大批次数 }  func (g *HookSecurityGuard) CheckWebhook(urlStr string) error {     u, _ := url.Parse(urlStr)      // 禁止向以下地址发送 webhook     blocked := []string{"127.0.0.1", "localhost", "10.", "172.16.", "192.168.", "0.0.0.0"}     for _, b := range blocked {         if strings.Contains(u.Host, b) {             return fmt.Errorf("webhook to internal network is blocked: %s", u.Host)         }     }      // 仅允许 HTTPS (生产环境)     if os.Getenv("GIN_MODE") == "release" && u.Scheme != "https" {         return fmt.Errorf("webhook must use HTTPS in production")     }      return nil } ```
