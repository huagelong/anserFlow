> 来源：`docs/09-监控与反馈闭环.md` 第 186 行 > 位置：[总目录](../../README.md) -> [09 - 监控与反馈闭环](../README.md) -> 当前章节 > 相邻：[上一篇](触发条件可配置.md) | [下一篇](../9.5-监控面板/README.md) > 相关主题：[05 - 工作流引擎](../../05-工作流引擎/README.md) | [03 - 数据模型](../../03-数据模型/README.md) | [04 - API 设计](../../04-API设计/README.md) | [10 - 部署与运维](../../10-部署与运维/README.md) ### 自动生成 Issue 内容  ```go // internal/service/monitor/issue_builder.go  func buildIssueFromEvent(event MonitorEvent, recentCount int, settings ProjectSettings) *CreateIssueInput {     metadata := event.Metadata     return &CreateIssueInput{         Title: fmt.Sprintf("[Monitor] %s: %s", event.EventType, truncate(event.Message, 100)),         Description: fmt.Sprintf(` ## 监控自动创建  ### 错误信息 %s  ### 堆栈跟踪 `+"```"+` %s `+"```"+`  ### 上下文 | 字段 | 值 | |------|-----| | 来源 | %s | | URL | %s | | 方法 | %s | | 环境 | %s | | 版本 | %s |  ### 统计 - 最近 1 小时出现次数: %d - 首次出现: %s - 最近出现: %s  --- > 此 Issue 由 flowcode 监控系统自动创建 > 事件ID: %s `, event.Message,             orDefault(event.StackTrace, "无堆栈信息"),             event.Source,             orDefault(metadata["url"], "-"),             orDefault(metadata["method"], "-"),             orDefault(metadata["environment"], "-"),             orDefault(metadata["releaseVersion"], "-"),             recentCount,             event.FirstSeenAt.Format(time.RFC3339),             event.LastSeenAt.Format(time.RFC3339),             event.ID,         ),         Priority: inferPriority(event, recentCount, settings),         Category: "bug",         Tags:     []string{"monitor", fmt.Sprintf("source:%s", event.Source)},         Source:   "monitor",     } }  func truncate(s string, maxLen int) string {     runes := []rune(s)     if len(runes) <= maxLen {         return s     }     return string(runes[:maxLen]) + "..." }  func orDefault(val, defaultVal string) string {     if val == "" {         return defaultVal     }     return val } ``` 
