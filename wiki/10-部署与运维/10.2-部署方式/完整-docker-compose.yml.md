> 来源：`docs/10-部署与运维.md` 第 24 行 > 位置：[总目录](../../README.md) -> [10 - 部署与运维](../README.md) -> 当前章节 > 相邻：[上一篇](方式-B：Docker-ComposeMySQL-生产环境.md) | [下一篇](Nginx-配置生产环境推荐.md) > 相关主题：[02 - 系统架构](../../02-系统架构/README.md) | [07 - AI 编程工具集成](../../07-AI编程工具集成/README.md) | [09 - 监控与反馈闭环](../../09-监控与反馈闭环/README.md) | [11 - 测试策略](../../11-测试策略/README.md) ### 完整 docker-compose.yml  ```yaml version: '3.8'  services:   # MySQL 数据库（SQLite 模式下可移除本 service）   mysql:     image: mysql:8.0     container_name: flowcode-db     restart: unless-stopped     environment:       MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-flowcode_root}       MYSQL_DATABASE: flowcode       MYSQL_USER: flowcode       MYSQL_PASSWORD: ${DB_PASSWORD:-flowcode_secret}     volumes:       - mysql_data:/var/lib/mysql     ports:       - '127.0.0.1:3306:3306'     healthcheck:       test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost']       interval: 10s       timeout: 5s       retries: 5     profiles:       - mysql  # 仅 --profile mysql 时启动    # Redis (缓存 + 任务队列)   redis:     image: redis:7-alpine     container_name: flowcode-redis     restart: unless-stopped     command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru     volumes:       - redis_data:/data     ports:       - '127.0.0.1:6379:6379'     healthcheck:       test: ['CMD', 'redis-cli', 'ping']       interval: 10s       timeout: 3s       retries: 5    # flowcode Server (Go 二进制)   server:     image: flowcode/server:latest     container_name: flowcode-server     restart: unless-stopped     depends_on:       redis:         condition: service_healthy       mysql:         condition: service_healthy         required: false  # SQLite 模式下不需要 MySQL     environment:       GIN_MODE: release       FLOWCODE_DB_DRIVER: ${FLOWCODE_DB_DRIVER:-mysql}       FLOWCODE_MYSQL_DSN: ${FLOWCODE_MYSQL_DSN:-flowcode:flowcode_secret@tcp(mysql:3306)/flowcode?charset=utf8mb4&parseTime=True}       REDIS_URL: redis://redis:6379       FLOWCODE_ENCRYPTION_KEY: ${ENCRYPTION_KEY:-change-me-to-a-random-32-char-string}       JWT_SECRET: ${JWT_SECRET:-change-me-to-a-random-secret}       SANDBOX_IMAGE: flowcode/sandbox:latest   # 沙盒容器镜像       DOCKER_HOST: unix:///var/run/docker.sock  # Docker SDK 连接方式       LOG_LEVEL: info     volumes:       - /var/run/docker.sock:/var/run/docker.sock  # Docker SDK       - skills_data:/app/skills                     # Skills 文件存储     ports:       - '127.0.0.1:3000:3000'     healthcheck:       test: ['CMD', 'wget', '--quiet', '--tries=1', '--spider', 'http://localhost:3000/api/v1/health']       interval: 30s       timeout: 5s       retries: 3    # Nginx (反向代理 + 前端静态资源)   nginx:     image: nginx:alpine     container_name: flowcode-nginx     restart: unless-stopped     ports:       - '80:80'       - '443:443'     volumes:       - ./nginx.conf:/etc/nginx/nginx.conf       - ./dist:/usr/share/nginx/html   # Vue 3 构建产物       - ./ssl:/etc/nginx/ssl           # SSL 证书 (可选)     depends_on:       - server  volumes:   mysql_data:   redis_data:   skills_data:   # no sandbox_data — Docker manages containers & volumes internally  # ============================================ # 部署前需先构建沙盒镜像: #   仓库根目录已提供 Dockerfile.sandbox（Node 22 slim + Python + Go） #   docker build -f Dockerfile.sandbox -t flowcode/sandbox:latest . # ============================================ ``` 
