> 来源：`docs/10-部署与运维.md` 第 128 行 > 位置：[总目录](../../README.md) -> [10 - 部署与运维](../README.md) -> 当前章节 > 相邻：[上一篇](完整-docker-compose.yml.md) | [下一篇](方式-C：npm-全局安装CLI-工具.md) > 相关主题：[02 - 系统架构](../../02-系统架构/README.md) | [07 - AI 编程工具集成](../../07-AI编程工具集成/README.md) | [09 - 监控与反馈闭环](../../09-监控与反馈闭环/README.md) | [11 - 测试策略](../../11-测试策略/README.md) ### Nginx 配置（生产环境推荐）  ```nginx # nginx.conf worker_processes auto; error_log /var/log/nginx/error.log warn; pid /var/run/nginx.pid;  events {     worker_connections 1024; }  http {     include       /etc/nginx/mime.types;     default_type  application/octet-stream;      log_format main '$remote_addr - $remote_user [$time_local] "$request" '                     '$status $body_bytes_sent "$http_referer" '                     '"$http_user_agent"';     access_log /var/log/nginx/access.log main;      sendfile        on;     keepalive_timeout 65;     gzip            on;     gzip_types      text/plain text/css application/json application/javascript text/xml application/xml;      # 后端 upstream — 多实例负载均衡     upstream flowcode_backend {         ip_hash;                         # WebSocket 会话保持         server flowcode-1:3000;         server flowcode-2:3000;         server flowcode-3:3000;         keepalive 32;     }      server {         listen 80;         server_name flowcode.example.com;          # 安全头         add_header X-Frame-Options "DENY" always;         add_header X-Content-Type-Options "nosniff" always;         add_header Referrer-Policy "strict-origin-when-cross-origin" always;          # API 反向代理         location /api/ {             proxy_pass http://flowcode_backend;             proxy_http_version 1.1;             proxy_set_header Host $host;             proxy_set_header X-Real-IP $remote_addr;             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;             proxy_set_header X-Forwarded-Proto $scheme;         }          # WebSocket 代理 (ip_hash 会话保持 + Redis Pub/Sub 跨实例)         location /ws/ {             proxy_pass http://flowcode_backend;             proxy_http_version 1.1;             proxy_set_header Upgrade $http_upgrade;             proxy_set_header Connection "upgrade";             proxy_set_header Host $host;             proxy_set_header X-Real-IP $remote_addr;             proxy_read_timeout 3600s;             proxy_send_timeout 3600s;         }          # SDK 静态资源         location /sdk/ {             proxy_pass http://server:3000;             proxy_http_version 1.1;             proxy_set_header Host $host;         }          # 前端静态资源 (Vue 3 构建产物)         location / {             root   /usr/share/nginx/html;             index  index.html;             try_files $uri $uri/ /index.html;         }     } } ``` 
