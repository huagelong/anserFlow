# 02 - 系统架构

## 2.1 架构全景图

```
                               ┌─────────────────────────┐
                               │     Web UI (Vue 3)      │
                               │  Dashboard / Kanban /   │
                               │  IssueDetail / Settings │
                               └───────────┬─────────────┘
                                           │ HTTP/WS
                               ┌───────────┴─────────────┐
                               │    Nginx (反向代理)       │
                               │   (静态资源 + API 代理)   │
                               └───────────┬─────────────┘
                                           │
                               ┌───────────┴─────────────┐
                               │    API Gateway           │
                               │   (Gin + Middleware)     │
                               └───────────┬─────────────┘
                                           │
            ┌──────────────────────────────┼──────────────────────────────┐
            │                              │                              │
   ┌────────┴────────┐           ┌────────┴────────┐           ┌─────────┴─────────┐
   │  Core Services   │           │  Task Engine     │           │  Integration Hub  │
   │  (Go Modules)    │           │  (Go Goroutines) │           │  (Go Interfaces)  │
   │                  │           │                  │           │                   │
   │ • RequirementSvc │◄─────────►│ • JobScheduler   │◄─────────►│ • AIToolAdapter   │
   │ • IssueService   │           │ • ExecutionQueue │           │ • GitProvider     │
   │ • Classifier     │           │ • SandboxMgr     │           │ • MonitorCollector│
   │ • PriorityEngine │           │ • LogStreamer    │           │ • WebhookRelay    │
   │ • UserService    │           │ • RetryPolicy    │           │ • SkillLoader     │
   │ • OrgService     │           │ • TestRunner     │           │ • CLIService      │
   │ • SkillService   │           │                  │           │                   │
   └────────┬─────────┘           └────────┬─────────┘           └─────────┬─────────┘
            │                              │                              │
            └──────────────────────────────┼──────────────────────────────┘
                                           │
                               ┌───────────┴─────────────┐
                               │      Data Layer          │
                               │  ┌───────────────────┐   │
                               │  │  MySQL 8.0 (主库)  │   │
                               │  └───────────────────┘   │
                               │  ┌───────────────────┐   │
                               │  │  Redis 7 (缓存/队列) │   │
                               │  └───────────────────┘   │
                               │  ┌───────────────────┐   │
                               │  │  本地文件存储       │   │
                               │  │  (沙盒/产物/Skills) │   │
                               │  └───────────────────┘   │
                               └───────────────────────────┘
```

## 2.2 技术选型

| 层级 | 技术 | 选型理由 |
|------|------|---------|
| **前端** | Vue 3 + TypeScript | 渐进式框架，学习曲线平缓，适合小团队 |
| **构建工具** | Vite 5 | 极速 HMR，开发体验好 |
| **UI 组件库** | Element Plus | 成熟的企业级 Vue 3 组件库，中文生态好 |
| **状态管理** | Pinia | Vue 3 官方推荐，TypeScript 友好 |
| **路由** | Vue Router 4 | Vue 3 官方路由方案 |
| **后端框架** | Go + Gin | 高性能、低内存、编译型语言，部署简单 |
| **ORM** | Gorm v2 | Go 生态最成熟的 ORM，AutoMigrate 方便 |
| **数据库** | MySQL 8.0 | 广泛使用，JSON 类型支持，运维成熟 |
| **缓存/队列** | Redis 7 + Asynq | Go 原生任务队列（基于 Redis），轻量可靠 |
| **实时通信** | WebSocket (gorilla/websocket) | 双向通信，推送执行日志 |
| **文件存储** | 本地文件系统 | 沙盒产物、Skills 文件、日志归档 |
| **CLI 工具** | Go + Cobra | Go 生态标准 CLI 框架 |
| **容器化** | Docker + docker-compose | 一键部署 |
| **反向代理** | Nginx | 高性能反向代理，静态资源服务 |
| **CI/CD** | GitHub Flow + GitHub Actions | 标准分支策略 + 自动化构建 |
| **Git 操作** | Git CLI + go-git | git clone/push 常用 go-git（纯 Go 零依赖），ls-remote/rebase 降级 CLI |
| **Git 平台 SDK** | go-github + go-gitlab + go-gitea | PR/MR/Webhook 操作，三个平台均为必需依赖 |
| **状态机** | looplab/fsm | Go 生态最成熟的轻量 FSM，声明式事件+回调，Graphviz 可视化 |
| **沙盒容器** | Docker SDK | docker/client 官方包，容器内安全执行 AI 生成代码 |

## 2.3 模块详细划分

### 2.3.1 前端模块 (web/)

```
web/
├── src/
│   ├── views/                     # 页面视图
│   │   ├── Login.vue              # 登录 + 组织选择
│   │   ├── Dashboard.vue          # 项目仪表盘
│   │   ├── ProjectList.vue        # 项目列表 / 创建
│   │   ├── ProjectCreate.vue      # 创建项目 (Git URL 校验 → 分支选择)
│   │   ├── Requirements.vue       # 需求列表
│   │   ├── RequirementDetail.vue  # 需求详情 / 拆解为 Issue
│   │   ├── Issues.vue             # Issue 看板
│   │   ├── IssueDetail.vue        # Issue 详情 / 执行日志
│   │   ├── PullRequests.vue       # PR 列表与状态追踪
│   │   ├── TestCases.vue          # 测试用例列表
│   │   ├── TestCaseDetail.vue     # 测试用例详情: 脚本编辑器 + 结果卡片
│   │   ├── TestCaseHistory.vue    # 测试用例版本 diff / 回滚
│   │   ├── History.vue            # Issue/需求变更历史
│   │   ├── HistoryDiff.vue        # 双栏字段级 diff 对照 (Monaco)
│   │   ├── Settings.vue           # 系统设置 / AI工具 / Git凭证 / 插件
│   │   ├── Monitor.vue            # 监控面板
│   │   ├── Skills.vue             # Skills 管理 (列表/创建/编辑/发布)
│   │   ├── AuditLog.vue           # CLI/Web 操作审计日志
│   │   ├── DeviceManagement.vue   # 登录设备列表 / 远程吊销
│   │   └── Organization.vue       # 组织管理 (成员/项目/配额)
│   ├── components/
│   │   ├── kanban/                # 看板列 + 拖拽卡片 (vuedraggable)
│   │   ├── issue/                 # Issue 表单/卡片/分类徽章
│   │   ├── execution/             # 执行日志流 (xterm + WebSocket)
│   │   ├── editor/                # Markdown / testScript 代码编辑器 (Monaco)
│   │   ├── skill/                 # Skill 编辑器/预览/同步按钮
│   │   ├── diff/                  # Diff 渲染 (diff2html) + 回滚确认弹窗
│   │   ├── git/                   # Git URL 校验组件 / 分支选择器
│   │   ├── test/                  # 测试结果卡片 (通过/失败/覆盖率)
│   │   └── shared/                # 通用 UI 组件 (Element Plus 封装)
│   ├── composables/               # Vue 3 组合式函数 (useWebSocket, useDiff, ...)
│   ├── stores/                    # Pinia stores (project, issue, testCase, ...)
│   ├── router/                    # Vue Router 路由定义
│   └── api/                       # API 调用层 (axios)
```

**路由概览**：

| 路径 | 页面 | 说明 |
|------|------|------|
| `/login` | Login.vue | 登录 + 组织选择 |
| `/` | Dashboard.vue | 项目仪表盘（最近 Issue / PR / 覆盖率概览） |
| `/projects` | ProjectList.vue | 项目列表 + 新建入口 |
| `/projects/new` | ProjectCreate.vue | 创建项目（Git URL 校验 + 分支选择） |
| `/projects/:pid/requirements` | Requirements.vue | 需求列表 |
| `/projects/:pid/requirements/:rid` | RequirementDetail.vue | 需求详情 + 拆解为 Issue |
| `/projects/:pid/issues` | Issues.vue | Issue 看板（按 FSM 状态分列） |
| `/projects/:pid/issues/:iid` | IssueDetail.vue | Issue 详情 + 执行日志实时流 |
| `/projects/:pid/issues/:iid/history` | History.vue | Issue 变更历史列表 |
| `/projects/:pid/issues/:iid/history/diff/:hid` | HistoryDiff.vue | 字段级双栏 diff 对照 |
| `/projects/:pid/prs` | PullRequests.vue | PR 列表与状态 |
| `/projects/:pid/test-cases` | TestCases.vue | 测试用例列表 |
| `/projects/:pid/test-cases/:tid` | TestCaseDetail.vue | 测试详情 + 脚本编辑器 + 结果 |
| `/projects/:pid/test-cases/:tid/history` | TestCaseHistory.vue | 测试用例版本 diff / 回滚 |
| `/projects/:pid/settings` | Settings.vue | AI 工具 / Git 凭证 / 标签 / API Key |
| `/projects/:pid/monitor` | Monitor.vue | 监控事件 + 错误趋势 |
| `/projects/:pid/skills` | Skills.vue | Skills 管理 |
| `/projects/:pid/audit-log` | AuditLog.vue | CLI / Web 操作审计 |
| `/org/:slug` | Organization.vue | 组织管理（成员 / 项目 / 设备） |
| `/org/:slug/devices` | DeviceManagement.vue | 已登录设备列表 / 注销 |

**侧边栏导航结构**：

```
┌─ 项目侧边栏 ──────────────────────┐
│ 📊 仪表盘 (Dashboard)              │
│ 📋 Issue 看板 (Issues)             │
│ 📝 需求管理 (Requirements)          │
│ 🔀 PR 管理 (Pull Requests)         │
│ 🧪 测试用例 (Test Cases)           │
│ 📈 监控面板 (Monitor)              │
│ ───────────────────────────────   │
│ ⚙️ 项目设置 (Settings)             │
│   ├── AI 工具配置                  │
│   ├── Git 凭证                    │
│   ├── 标签管理                    │
│   ├── API Key                     │
│   └── 📦 Skills 管理              │
│ 📜 审计日志 (Audit Log)            │
│ 🏢 组织管理 (Organization)         │
│   └── 📱 设备管理 (Devices)       │
└────────────────────────────────────┘
```

### 2.3.2 后端模块 (server/)

```
server/
├── cmd/
│   ├── server/                    # Web 服务入口
│   │   └── main.go
│   └── cli/                       # CLI 工具入口
│       └── main.go
├── internal/
│   ├── handler/                   # Gin HTTP Handler 层
│   │   ├── auth.go
│   │   ├── requirement.go
│   │   ├── issue.go
│   │   ├── execution.go
│   │   ├── git.go
│   │   ├── monitor.go
│   │   ├── ai_tool.go
│   │   ├── skill.go
│   │   ├── organization.go
│   │   └── api_key.go
│   ├── service/                   # 业务逻辑层
│   │   ├── requirement.go
│   │   ├── issue.go
│   │   ├── execution.go
│   │   ├── classifier.go
│   │   ├── git.go
│   │   ├── git_url_validator.go    # Git 地址可达性校验 (HTTP/SSH)
│   │   ├── monitor.go
│   │   ├── skill.go
│   │   ├── organization.go
│   │   ├── sandbox/              # Docker 容器沙盒
│   │   │   ├── docker.go         # 容器创建/销毁/Exec
│   │   │   ├── config.go         # SandboxConfig
│   │   │   ├── prepare.go        # clone + skills 注入
│   │   │   └── commit.go         # 容器内 git commit/push
│   │   ├── test_runner.go         # 测试用例生成与执行
│   ├── fsm/                        # 状态机 (looplab/fsm)
│   │   ├── issue_fsm.go            # Issue 状态机定义
│   │   ├── issue_callbacks.go      # Issue 回调 (审核/执行/PR/重试)
│   │   ├── test_case_fsm.go        # TestCase 状态机定义
│   │   ├── test_case_callbacks.go  # TestCase 回调 (审核/生成/运行)
│   │   ├── hook_events.go          # 全局 Hook 事件常量
│   │   ├── history_hook.go         # 历史记录自动采集回调
│   │   ├── persistence.go          # 状态持久化 + DB 恢复
│   │   └── dependencies.go         # FSMDependencies 依赖注入
│   ├── model/                     # Gorm 数据模型
│   │   ├── user.go
│   │   ├── organization.go
│   │   ├── project.go
│   │   ├── requirement.go
│   │   ├── issue.go
│   │   ├── execution_log.go
│   │   ├── test_case.go
│   │   ├── skill.go
│   │   ├── api_key.go
│   │   ├── git_credential.go
│   │   ├── ai_tool_config.go
│   │   └── monitor_event.go
│   ├── adapter/                   # 适配器层
│   │   ├── ai_tool/               # AI 工具适配器
│   │   │   ├── adapter.go         # 接口定义
│   │   │   ├── opencode.go
│   │   │   ├── codex.go
│   │   │   ├── claude_code.go
│   │   │   └── classify.go        # AI 分类适配器（关键词不足时补充分类）
│   │   └── git/                   # Git Provider 适配器（三个平台必需）
│   │       ├── provider.go        # PlatformProvider 接口定义
│   │       ├── factory.go         # 工厂函数 NewPlatformProvider()
│   │       ├── github.go          # go-github SDK
│   │       ├── gitlab.go          # go-gitlab SDK
│   │       └── gitea.go           # go-gitea SDK
│   ├── middleware/                 # Gin 中间件
│   │   ├── auth.go                # JWT / API Key 鉴权
│   │   ├── tenant.go              # 租户识别 + orgId 注入
│   │   ├── cors.go
│   │   ├── ratelimit.go
│   │   └── logger.go
│   ├── queue/                     # Asynq 任务队列
│   │   ├── client.go
│   │   ├── server.go
│   │   ├── tasks.go               # 任务定义
│   │   ├── classify_handler.go    # AI 异步分类处理器
│   │   └── execute_handler.go     # AI 执行处理器
│   ├── ws/                        # WebSocket
│   │   └── hub.go
│   ├── config/
│   │   └── config.go              # Viper 配置管理
│   └── database/
│       ├── mysql.go               # MySQL 连接 + Gorm 初始化
│       └── redis.go               # Redis 连接
├── migrations/                    # Gorm AutoMigrate 或 SQL 迁移文件
└── go.mod
```

### 2.3.3 CLI 模块 (cmd/cli/)

```
cmd/cli/
├── main.go                        # CLI 入口 (Cobra)
├── cmd/
│   ├── root.go                    # 根命令
│   ├── server.go                  # flowcode server (启动 Web 服务)
│   ├── login.go                   # flowcode login
│   ├── init.go                    # flowcode init (一键初始化项目)
│   ├── sync.go                    # flowcode sync (同步 config + skills 到 Git)
│   ├── issue.go                   # flowcode issue [create|list|search|close|watch|...]
│   ├── requirement.go             # flowcode req [create|list|convert]
│   ├── test.go                    # flowcode test [run|result|list|watch]
│   ├── workflow.go                # flowcode workflow [status|next|rollback]
│   ├── history.go                 # flowcode history [list|diff|rollback]
│   ├── pr.go                      # flowcode pr [create|list|status|merge]
│   ├── skill.go                   # flowcode skill [install|list|create]
│   ├── project.go                 # flowcode project [list]
│   ├── stats.go                   # flowcode stats (项目健康面板)
│   ├── audit.go                   # flowcode audit-log
│   └── config.go                  # flowcode config [set|get]
└── pkg/
    ├── client/                    # API 客户端封装
    │   └── api.go
    └── output/                    # 输出格式化 (JSON/Table/Color)
        └── printer.go
```

**命令总览**：

| 命令组 | 子命令 | 说明 | 优先级 |
|--------|--------|------|--------|
| `login` | — | 登录认证，设备注册 | 🔴 核心 |
| `server` | — | 启动 flowcode Web 服务（API + WebSocket） | 🔴 核心 |
| `init` | — | 一键初始化：创建 `.flowcode/config.yaml` + sync skills + git commit | 🔴 高 |
| `sync` | — | 将 skills + config.yaml 写入项目 Git 仓库 | 🔴 高 |
| `issue` | `create`, `list`, `search`, `close`, `watch` | Issue 全生命周期管理 | 🔴 核心 |
| `test` | `run`, `result`, `list`, `watch` | 测试执行与结果查询 | 🔴 高 |
| `workflow` | `status`, `next`, `rollback` | 工作流状态查看与推进 | 🟡 中 |
| `history` | `list`, `diff`, `rollback` | 历史记录查看 / 字段级 diff / 版本回滚 | 🟡 中 |
| `req` | `create`, `list`, `convert` | 需求管理 | 🟡 中 |
| `stats` | — | 项目健康面板：Issue 数 / 测试通过率 / 覆盖率 / 阻塞项 | 🟡 中 |
| `pr` | `create`, `list`, `status`, `merge` | PR 创建、列表、状态追踪与合并 | 🟡 中 |
| `skill` | `install`, `list`, `create` | Skill 管理 | 🟢 低 |
| `project` | `list` | 项目列表 | 🟢 低 |
| `config` | `set`, `get` | 本地配置管理 | 🟢 低 |
| `audit-log` | — | 查看 CLI 操作审计记录 | 🟢 低 |

**典型使用场景**：

```bash
# 1. 初始化项目（首次接入 flowcode）
flowcode init
# → ✓ Created .flowcode/config.yaml
# → ✓ Synced 7 skills to .flowcode/skills/
# → ✓ Committed & pushed to git

# 2. 查看项目概览
flowcode stats
# → Issues: 12 open / 5 in review / 3 blocked
# → Tests: 78% pass rate / 62% coverage
# → PRs: 2 open

# 3. 创建 Issue 并查看工作流状态
flowcode issue create -t "用户登录页" -d "实现邮箱+密码登录" -p P1
# → Created: iss_abc123
flowcode workflow status iss_abc123
# → State: draft | Next: submit / cancel

# 4. 运行测试
flowcode test run tc_xyz --watch
# → [实] 实时流式输出测试日志...

# 5. 查看历史并回滚
flowcode history list --issue iss_abc123
flowcode history rollback h_old_001 --confirm

# 6. PR 管理
flowcode pr list                          # 项目所有 PR
flowcode pr list --status open            # 只看打开的
flowcode pr create --issue iss_abc123 --branch feat/login
flowcode pr merge --issue iss_abc123       # 合并 PR
```

### 2.3.4 Skills 目录约定

```
skills/                            # 项目级或全局 Skills 存储
├── go-best-practices/
│   ├── skill.md                   # Skill 定义 (Markdown)
│   └── examples/                  # 示例代码
├── react-component-pattern/
│   └── skill.md
└── api-error-handling/
    └── skill.md
```

### 2.3.5 CLI 安全机制

CLI 工具直接操作云端的 Issue、触发执行、安装 Skill，安全性至关重要。

#### 2.3.5a Token 安全存储

```go
// internal/cli/auth/store.go

// 凭证按 OS 标准路径加密存储
// Linux:   ~/.config/flowcode/auth.json (权限 0600)
// macOS:   ~/Library/Application Support/flowcode/auth.json
// Windows: %APPDATA%/flowcode/auth.json

type CredentialStore struct {
    path string
    key  []byte // 从机器特征派生 (hostname + machine-id + os)
}

func (s *CredentialStore) Save(cred *Credentials) error {
    data, _ := json.Marshal(cred)
    // AES-256-GCM 加密后写入文件
    encrypted := aesGCMEncrypt(data, s.key)
    return os.WriteFile(s.path, encrypted, 0600)
}

func (s *CredentialStore) Load() (*Credentials, error) {
    encrypted, err := os.ReadFile(s.path)
    if err != nil {
        return nil, fmt.Errorf("not logged in, run 'flowcode login' first")
    }
    return aesGCMDecrypt(encrypted, s.key)
}

type Credentials struct {
    AccessToken  string    `json:"access_token"`
    RefreshToken string    `json:"refresh_token"`
    ServerURL    string    `json:"server_url"`
    ExpiresAt    time.Time `json:"expires_at"`
    DeviceID     string    `json:"device_id"`
}
```

#### 2.3.5b 设备注册与绑定

首次登录时，CLI 向服务端注册设备，后续操作可被审计追溯到具体设备：

```go
// internal/cli/auth/device.go

type DeviceInfo struct {
    DeviceID   string // SHA256(hostname + machine-id)
    DeviceName string // "MacBook Pro" / "CI Runner"
    OS         string // "darwin" / "linux" / "windows"
    Arch       string // "arm64" / "amd64"
}

func RegisterDevice(client *api.Client, device DeviceInfo) error {
    return client.Post("/api/v1/devices", device)
}
```

服务端记录设备列表，用户可在 Web 控制台查看并远程吊销已登录设备。

#### 2.3.5c 自动 Token 刷新

CLI 在请求前自动检查 Token 过期时间，透明刷新：

```go
// internal/cli/client/interceptor.go

func (c *Client) Do(req *http.Request) (*http.Response, error) {
    cred, _ := c.store.Load()

    // Token 将在 5 分钟内过期 → 提前刷新
    if time.Until(cred.ExpiresAt) < 5*time.Minute {
        newCred, err := c.refreshToken(cred.RefreshToken)
        if err != nil {
            return nil, fmt.Errorf("session expired, run 'flowcode login'")
        }
        c.store.Save(newCred)
        cred = newCred
    }

    req.Header.Set("Authorization", "Bearer "+cred.AccessToken)
    req.Header.Set("X-Device-ID", cred.DeviceID)
    return c.http.Do(req)
}
```

#### 2.3.5d CLI 操作审计

所有 CLI 发起的敏感操作记录在服务端的 AuditLog 中，authMethod 标记为 `cli`：

```bash
# 用户可在 Web 控制台查看自己的 CLI 操作历史
flowcode audit-log --since 7d
```

#### 2.3.5e CLI 安全最佳实践

| 措施 | 说明 |
|------|------|
| 本地凭证加密 | 使用 AES-256-GCM + 机器特征派生密钥，离开本机无法解密 |
| 文件权限 | 凭证文件 chmod 0600，仅当前用户可读写 |
| Token 过期 | Access Token 2h，Refresh Token 30d 滚动刷新 |
| 设备注册 | 每次登录注册设备，Web 端可远程吊销 |
| 操作签名 | 敏感操作 (execute/skill install) 附带 HMAC 签名 |
| 审计日志 | 所有 CLI 操作记录到服务端审计表，可追溯 |
| 输入消毒 | CLI 参数在发送前做 trim + 转义处理 |

## 2.4 SaaS 多租户架构

### 2.4.1 隔离模型：Shared DB + orgId

flowcode 采用 **共享数据库 + orgId 过滤** 的轻量多租户模型。一张 MySQL 服务所有组织，通过 Gorm Scoped 查询自动注入 `WHERE org_id = ?`。

```
┌────────────────────────────────────────────────────────┐
│                    MySQL (单实例)                        │
│                                                         │
│  ┌─────────┐ ┌──────────┐ ┌────────┐ ┌───────────┐   │
│  │ Projects │ │  Issues  │ │  Users │ │ OrgMembers│   │
│  │  org_id  │ │  org_id  │ │        │ │  org_id   │   │
│  └─────────┘ └──────────┘ └────────┘ └───────────┘   │
│                                                         │
│  Org A 的数据: WHERE org_id = 'org-aaa'                 │
│  Org B 的数据: WHERE org_id = 'org-bbb'                 │
└────────────────────────────────────────────────────────┘
```

> 面向独立开发者和小团队，Shared DB 是最优解。若未来有企业合规需求，可通过 `DB_PER_TENANT` 模式扩展。

### 2.4.2 租户识别：JWT + APIKey（无 Subdomain）

不做域名路由，直接通过请求中携带的凭证识别 orgId：

```go
// internal/middleware/tenant.go

func TenantMiddleware() gin.HandlerFunc {
    return func(c *gin.Context) {
        var orgID string

        // 1. JWT（Web 登录用户，claims 中携带当前 orgId）
        if claims := GetJWTClaims(c); claims != nil {
            orgID = claims.OrgID
        }

        // 2. API Key（CLI / CI 调用，key 本身绑定到 org）
        if apiKey := c.GetHeader("X-FlowCode-API-Key"); apiKey != "" {
            token, err := findAPIToken(c, apiKey)
            if err == nil {
                orgID = token.OrgID
            }
        }

        // 3. 组织切换：用户属于多个组织时，通过 Header 切换
        if switchOrg := c.GetHeader("X-FlowCode-Org-Id"); switchOrg != "" {
            if isMember(c, switchOrg) {
                orgID = switchOrg
            }
        }

        if orgID == "" {
            c.AbortWithStatusJSON(401, gin.H{"error": "tenant not identified"})
            return
        }

        c.Set("orgID", orgID)
        c.Next()
    }
}
```

**识别优先级**：`X-FlowCode-Org-Id`（显式切换）> JWT claims > API Key

### 2.4.3 Gorm Scoped 查询

所有数据库查询通过统一入口自动注入 orgId 过滤，防止跨租户数据泄露：

```go
// internal/service/base.go

func (s *BaseService) DB(ctx context.Context) *gorm.DB {
    orgID := ctx.Value("orgID").(string)
    return s.db.WithContext(ctx).Where("org_id = ?", orgID)
}

// 使用：所有 Service 通过 DB(ctx) 替代 db
func (s *IssueService) List(ctx context.Context, projectID string) ([]Issue, error) {
    var issues []Issue
    // SQL: SELECT * FROM issues WHERE org_id = ? AND project_id = ?
    return issues, s.DB(ctx).Where("project_id = ?", projectID).Find(&issues).Error
}
```

## 2.5 数据流

### 标准流程：需求 → Issue → 执行 → 测试 → PR → 部署

```
[用户/CLI/API] ─创建需求─→ [RequirementService] ─拆解─→ [IssueService]
                                                              │
                                                       ┌──────┴──────┐
                                                       │  管理员审核    │
                                                       └──────┬──────┘
                                                              │ approved
                                                       ┌──────┴──────┐
                                                       │ JobScheduler │
                                                       │  → Asynq     │
                                                       └──────┬──────┘
                                                              │
                                                  ┌───────────┴───────────┐
                                                  │    AIToolAdapter       │
                                                  │  (Docker 沙盒执行)      │
                                                  │  ┌─────────────────┐   │
                                                  │  │ opencode/codex/  │   │
                                                  │  │ claude-code      │   │
                                                  │  └────────┬────────┘   │
                                                  │           │            │
                                                  │  SkillLoader           │
                                                  │  SandboxManager(Docker)│
                                                  │  LogStreamer → WS      │
                                                  └───────────┬───────────┘
                                                              │ completed
                                                  ┌───────────┴───────────┐
                                                  │    TestRunner          │
                                                  │  → 自动生成测试用例     │
                                                  │  → 运行测试套件        │
                                                  └───────────┬───────────┘
                                                              │ passed
                                                  ┌───────────┴───────────┐
                                                  │     GitService         │
                                                  │  → cloneRepo           │
                                                  │  → createBranch        │
                                                  │  → commitChanges       │
                                                  │  → pushToRemote        │
                                                  │  → createPR            │
                                                  └───────────┬───────────┘
                                                              │ PR merged
                                                  ┌───────────┴───────────┐
                                                  │   Deploy → Monitor     │
                                                  │   → MonitorCollector   │
                                                  └───────────────────────┘
```

### 监控反馈流程：线上问题 → 自动Issue

```
[线上应用] ─异常事件─→ [MonitorController] → [EventProcessor]
                                                    │
                                           ┌────────┴────────┐
                                           │ 去重 + 分类 +    │
                                           │ 优先级推断       │
                                           └────────┬────────┘
                                                    │
                                           ┌────────┴────────┐
                                           │ 自动创建 Issue   │
                                           │ status=draft     │
                                           └─────────────────┘
```

## 2.6 部署架构 (GitHub Flow)

```
                        ┌─────────────────────────┐
                        │        Nginx             │
                        │  (反向代理 + 静态资源)     │
                        │  HTTP/2 + HTTPS           │
                        └─────────────┬───────────┘
                                      │
                    ┌─────────────────┴─────────────────┐
                    │                                   │
           ┌────────┴────────┐                  ┌───────┴──────────┐
           │   Web 容器       │                  │  Server 容器      │
           │  (Nginx + SPA)  │                  │  (Go 二进制)      │
           │  Vue 3 静态文件  │                  │  Gin API 服务     │
           └─────────────────┘                  └───────┬──────────┘
                                                        │
                          ┌─────────────────────────────┼────────────────┐
                          │                             │                │
                   ┌──────┴──────┐               ┌──────┴──────┐  ┌──────┴──────┐
                   │   MySQL 8   │               │   Redis 7   │  │  Docker 沙盒  │
                   │  (主数据库)  │               │ (缓存+队列)  │  │    dockerd   │
                   └─────────────┘               └─────────────┘  └─────────────┘
```

**GitHub Flow 分支策略**：
- `main` 分支始终保持可部署状态
- 功能开发从 `main` 拉出 `flowcode/<issue-id>-<slug>` 分支
- 通过 PR 合并回 `main`，触发 GitHub Actions 自动构建 Docker 镜像
- 所有服务通过 `docker-compose.yml` 编排，单机即可运行。最低 1C2G 云服务器。
